[package]
name = "vm"
version = "0.1.0"
edition = "2024"

[dependencies]
bytecode = { path = "../bytecode" }
dough_core = { path = "../dough_core" }
heap = { path = "../heap" }

[features]
trace = []
