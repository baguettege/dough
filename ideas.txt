static typed
register-based vm
closures
value + heap objects
main function

pub enum DoughValue {
    Unit,
    Int(i64),
    Float(f64),
    Bool(bool),
    Object(/*todo*/)
}

pub enum DoughObject {
    Str(/*todo*/),
    Function(/*todo*/),
    Closure(/*todo*/),
    Upvalue(/*todo*/)
}